name: 'news-and-stocks-dbt-postgre'
version: '1.0'

profile: 'news-and-stocks-dbt-postgre'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  news_and_stocks_dbt_postgre:
    bronze:
      +schema: bronze
      +materialized: table
    silver:
      +schema: silver
      +materialized: table
    gold:   
      +schema: gold
      +materialized: table 

seeds:
  news_and_stocks_dbt_postgre:
    +schema: seeds 
    +materialized: table
    marts:
      +schema: marts